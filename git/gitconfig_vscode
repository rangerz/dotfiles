[alias]
    dft = difftool

[diff]
    tool = vscode

[difftool "vscode"]
    cmd = code --wait --diff "$LOCAL" "$REMOTE"

[merge]
    tool = vscode

[mergetool "vscode"]
    trustExitCode = true
    cmd = code --wait --merge "$REMOTE" "$LOCAL" "$BASE" "$MERGED"

[difftool]
    prompt = false
